<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Passwordsecurity" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/my_light_primary</item>
        <item name="colorSecondary">@color/my_light_primary</item>
        <item name="colorTertiary">@color/my_light_primary</item>
        <item name="android:colorBackground">@color/my_light_primary</item>
    </style>

<!-- TODO: define components style, stop here -->
    <style name="buttonRounded" parent="Widget.AppCompat.Button">

    </style>

    <style name="Theme.Passwordsecurity" parent="Base.Theme.Passwordsecurity" />
</resources>